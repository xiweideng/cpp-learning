#include <iostream>
using namespace std;

int main14()
{
    // comparison manipulation
    // ==,!=,>,<,<=,>=
    int a=1;
    int b=2;
    cout << (a==b) << endl;// use parentheses to change priority
    cout << (a!=b) << endl;// use parentheses to change priority
    cout << (a<b) << endl;// use parentheses to change priority
    cout << (a>b) << endl;// use parentheses to change priority
    cout << (a<=b) << endl;// use parentheses to change priority
    cout << (a>=b) << endl;// use parentheses to change priority

    return 0;
}
